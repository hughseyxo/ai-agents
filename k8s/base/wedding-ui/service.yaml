apiVersion: v1
kind: Service
metadata:
  name: wedding-ui
  namespace: wedding-ui
spec:
  type: NodePort
  selector:
    app: wedding-ui
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
