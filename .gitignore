.claude/*
!.claude/settings.json
.gemini/
.antigravitycli/
# BMAD installer artifacts — vendored third-party skills + machine-local
# install answers, regenerated by the installer. Not ours to version.
.agent/
_bmad/
_bmad-output/
output/*
!output/.gitkeep
output/librarian/
docs/superpowers/plans/librarian-arch-*.md
docs/.obsidian/
docs/.trash/
.env
credentials.json
mcp-servers/__pycache__/
agents/__pycache__/
data/agents.db
data/agents.db*
data/*.corrupt-bak
# Plant photo store + batch scratch dirs — hundreds of MB of personal photos
# carrying camera/EXIF metadata. Never publish to the public remote.
data/plant-photos/
data/plant-photo-batches/
docs/agent-notes.md
docs/security-audit-agent.md
docs/_map/
# LLM-written, unreviewed. Daily notes carry work topics, open threads and
# absolute host paths; agent-learnings is read back into agent prompts.
# Both stay on disk and keep syncing to Obsidian — they just aren't published.
docs/daily/
docs/agent-learnings/
*.mp4
*.mkv
*.avi
*.mov
__pycache__/
skills/mealsave/pot-provider/
skills/vidqueue/.venv/
.venv/
mcp-servers/.env.bridge

# Antigravity CLI per-repo conversation cache
cache/
data/wedding.db*
